[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# No public domain when using the nginx gateway (private networking only).
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
